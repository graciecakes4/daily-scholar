# keep the backend build context small + don't ship secrets/state
.git
.github
venv
__pycache__
*.pyc
*.pyo
.pytest_cache
.mypy_cache
.ruff_cache

# secrets / local state (never bake into images)
.env
.env.local

# data + uploads stay on a mounted volume in compose/Railway
data
uploads

# frontend is built in its own image
frontend
node_modules

# docs + dev artifacts
*.md
docs
PWA_PRAXIS_MIGRATION_PLAN.md
config/_archive
.Rhistory
.DS_Store
